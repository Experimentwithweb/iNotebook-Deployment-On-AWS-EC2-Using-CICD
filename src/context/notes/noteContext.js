// Purpose-created context API.

// Imported the create createContext from react to create the context api
import { createContext } from "react";

const noteContext = createContext(); // Created a note context api

export default noteContext;